type Slug = string;
type Method = 'GET' | 'POST' | 'PUT' | 'DELETE' | string;
type Remote = string;
type Status = number;
type User = string;

export {
    Slug,
    Method,
    Remote,
    Status,
    User
}